========================================
PERSON 3 AI - FINAL MULTI-SERVER SETUP
========================================

RUN THESE IN SEPARATE TERMINALS (in order):

========================================
TERMINAL A - Backend API (port 8016)
========================================
d:/Hackathon_Clavicular/.venv/Scripts/python.exe -m uvicorn ai_dev.src.server:app --host 0.0.0.0 --port 8016 --reload --log-level debug --access-log

========================================
TERMINAL B - Mock UI Server (port 8091)
========================================
Set-Location documentation/person3/mock-ui; d:/Hackathon_Clavicular/.venv/Scripts/python.exe -m http.server 8091 --bind 0.0.0.0

========================================
TERMINAL C - Cloudflared Tunnel (UI + Backend)
========================================
cloudflared tunnel --url http://127.0.0.1:8091

(Note: This makes UI publicly accessible via https://<random>.trycloudflare.com)
(UI has Base URL field - users set to http://192.168.51.28:8016 for backend)

========================================
ACCESS URLS:
========================================

YOUR COMPUTER (Local):
- UI: http://127.0.0.1:8091/index.html
- Backend: http://127.0.0.1:8016/health

SAME WIFI (Friends):
- UI: http://192.168.51.28:8091/index.html
- Backend: http://192.168.51.28:8016/health

PUBLIC (Cloudflared - from Terminal C output):
- UI: https://<random>.trycloudflare.com
- Backend in UI: Set Base URL field to http://192.168.51.28:8016

========================================
QUICK CLEANUP (if stuck):
========================================
Get-NetTCPConnection -LocalPort 8016 -ErrorAction SilentlyContinue | Stop-Process -Force -ErrorAction SilentlyContinue
Get-NetTCPConnection -LocalPort 8091 -ErrorAction SilentlyContinue | Stop-Process -Force -ErrorAction SilentlyContinue
Get-Process cloudflared -ErrorAction SilentlyContinue | Stop-Process -Force -ErrorAction SilentlyContinue

========================================
HOW FRIENDS USE THE UI:
========================================

1. Open: http://192.168.51.28:8091/index.html
2. See "Base URL" field at top (currently says localhost)
3. Change it to: http://192.168.51.28:8016
4. Click buttons: diagnose, sources, health check
5. See responses in output panel below

OR (from anywhere, via Cloudflared):
1. Wait for Terminal C to show: https://xxx.trycloudflare.com
2. Open that URL
3. Same as above - change Base URL to http://192.168.51.28:8016
4. Test endpoints

========================================
LOGGING:
========================================
- Terminal A: Shows all API requests/responses with timing
- Terminal B: Shows UI file access (GET /index.html, /styles, etc)
- Terminal C: Shows tunnel forwarding and remote requests

All timestamps sync for easy debugging.
